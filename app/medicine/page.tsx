import PageHeader from '../utils/PageHeader';
import Grid from './Grid';
import TalkToUs from '../utils/TalkToUs';

export default function Medicine() {
  return (
    <main>
      <PageHeader header='Medicine' />
      <Grid />
      <TalkToUs />
    </main>
  );
}
